import { DlHostDirective } from './dl-host.directive';

describe('DlHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DlHostDirective();
    expect(directive).toBeTruthy();
  });
});
